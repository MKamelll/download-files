### Cli to download stuff 🚌

Clone the repo `git clone https://github.com/MKamelll/download-files.git`

Run `npm install -g` in the folder.

Try it by typing in a terminal `dl "YOUR LINK HERE"` in the destination folder.
